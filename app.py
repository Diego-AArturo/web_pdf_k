import streamlit as st

def main():
    st.title("Gestión de Maquinas")

    # Maquina1
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina1", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

    #maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina2", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina2.py", label='Ir a recursos', use_container_width=True)

#maquina3
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina3", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina4", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina5", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina6", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina7", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina8", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina9", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)

#maquina2
    with st.container(border=True):
        # st.write("Bienvenido al gestor de mantenimiento. Aquí puedes acceder a diferentes recursos.")
        with st.container():
            st.header("Maquina10", divider="gray")
            
        with st.container():
            # st.write("**Acceso informacion :**")
            with st.container(border=True):
                st.page_link("pages/maquina1.py", label='Ir a recursos', use_container_width=True)


    
            

if __name__ == "__main__":
    main()




